let bcrypt;_393‍.x([["hashPassword",()=>hashPassword],["comparePassword",()=>comparePassword]]);_393‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

//functions for hashing and comparing password
//well hashed password and compare with the one in DB
//if the match allow user to login

       const hashPassword = (password) => {
  return new Promise((resolve, reject) => {
    bcrypt.genSalt(12, (err, salt) => {
      if (err) {
        reject(err);
      }
      bcrypt.hash(password, salt, (err, hash) => {
        if (err) {
          reject(err);
        }
        resolve(hash);
      });
    });
  });
};

       const comparePassword = (password, hashed) => {
  return bcrypt.compare(password, hashed);
};
